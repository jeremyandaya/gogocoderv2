<div class="container">
	<div class="row">
		<div class="col-md-12">
			<%= simple_form_for @code do |f| %>
				<%= f.input :title %>
				<%= f.input :content, class: "code-field" %>
				<%= f.button :submit %>
			<% end %>
		</div>
	</div>
</div>